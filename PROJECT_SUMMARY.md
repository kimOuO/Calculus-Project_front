# Calculus OOM 前端專案 - 完整說明

## 專案概述

這是一個完整的 Next.js 14 前端應用程式，用於微積分成績管理系統 (Calculus Out-of-Memory)。專案嚴格遵循 `frontend_architecture_prompt.md` 中定義的架構規範，並與後端 API 完全整合。

## 專案特點

### ✅ 架構規範
- **分層架構**: 嚴格的 app → components → hooks → services 分層
- **類型安全**: 完整的 TypeScript 類型定義 (strict mode)
- **單向依賴**: 下層不能引用上層，保持架構清晰
- **Server/Client 分離**: 適當使用 Next.js Server Components 和 Client Components

### ✅ 技術棧
- **Next.js 14** (App Router) - React 框架
- **React 18** - UI 庫
- **TypeScript 5.3** (strict mode) - 類型安全
- **Axios** - HTTP 客戶端
- **Tailwind CSS 3.4** - 樣式框架
- **ESLint + Prettier** - 程式碼品質工具

### ✅ 後端整合
- 完整支援所有後端 API (學生、成績、考試、檔案管理)
- 統一的錯誤處理機制
- 標準化的 API 回應處理

## 目錄結構

```
front/
├── app/                          # Next.js App Router (路由與頁面)
│   ├── layout.tsx               # 根布局
│   ├── page.tsx                 # 首頁
│   ├── globals.css              # 全域樣式
│   ├── students/page.tsx        # 學生管理頁面
│   ├── scores/page.tsx          # 成績管理頁面
│   └── tests/page.tsx           # 考試管理頁面
│
├── components/                   # UI 元件 (純展示)
│   ├── Button.tsx               # 按鈕元件
│   ├── Input.tsx                # 輸入框元件
│   ├── Select.tsx               # 下拉選單元件
│   ├── Modal.tsx                # 對話框元件
│   ├── Table.tsx                # 表格元件
│   ├── Badge.tsx                # 標籤元件
│   ├── LoadingSpinner.tsx       # 載入動畫
│   ├── StatusBadge.tsx          # 學生狀態標籤
│   ├── TestStateBadge.tsx       # 考試狀態標籤
│   ├── students/
│   │   ├── CreateStudentModal.tsx   # 新增學生對話框
│   │   └── StudentList.tsx          # 學生列表
│   ├── scores/
│   │   └── ScoreList.tsx            # 成績列表
│   └── tests/
│       └── TestList.tsx             # 考試列表
│
├── hooks/                        # React Hooks (狀態管理)
│   ├── base/                    # 基礎可重用 hooks
│   │   ├── useAsync.ts          # 非同步狀態管理
│   │   ├── usePagination.ts     # 分頁邏輯
│   │   ├── useSort.ts           # 排序邏輯
│   │   └── useFilter.ts         # 篩選邏輯
│   └── feature/                 # 功能特定 hooks
│       ├── useStudents.ts       # 學生相關 hooks
│       ├── useScores.ts         # 成績相關 hooks
│       ├── useTests.ts          # 考試相關 hooks
│       └── useFiles.ts          # 檔案相關 hooks
│
├── services/                     # API 服務層 (數據獲取)
│   ├── clients/
│   │   └── apiClient.ts         # Axios 客戶端配置
│   ├── api/
│   │   ├── studentApi.ts        # 學生 API
│   │   ├── scoreApi.ts          # 成績 API
│   │   ├── testApi.ts           # 考試 API
│   │   └── fileApi.ts           # 檔案 API
│   └── workflows/
│       └── index.ts             # 多 API 協調流程
│
├── types/                        # TypeScript 類型定義
│   ├── common.ts                # 通用類型 (AsyncState, ApiResponse 等)
│   ├── student.ts               # 學生類型
│   ├── score.ts                 # 成績類型
│   ├── test.ts                  # 考試類型
│   └── file.ts                  # 檔案類型
│
├── config/                       # 配置文件
│   ├── api.ts                   # API 端點配置
│   └── constants.ts             # 常數定義
│
├── public/                       # 靜態資源 (待添加)
├── prompt/                       # 架構文檔
│   ├── frontend_architecture_prompt.md    # 架構規範
│   └── FRONTEND_to_backend_API_RULES.md  # API 對接規範
│
├── package.json                  # 專案依賴
├── tsconfig.json                 # TypeScript 配置
├── next.config.mjs               # Next.js 配置
├── tailwind.config.js            # Tailwind CSS 配置
├── postcss.config.mjs            # PostCSS 配置
├── .eslintrc.json                # ESLint 配置
├── .prettierrc                   # Prettier 配置
├── .gitignore                    # Git 忽略文件
├── .env                          # 環境變數 (已設定)
├── .env.example                  # 環境變數範例
├── README.md                     # 專案說明
├── SETUP.md                      # 設置指南
└── start-dev.sh                  # 快速啟動腳本
```

## 功能模組

### 1. 學生管理 (/students)
- ✅ 查看學生列表
- ✅ 新增學生 (含表單驗證)
- ✅ 按學期篩選
- ✅ 學生狀態標籤顯示
- 🚧 編輯學生 (可擴展)
- 🚧 刪除學生 (可擴展)

### 2. 成績管理 (/scores)
- ✅ 查看成績列表
- ✅ 按學生 UUID 篩選
- ✅ 格式化分數顯示 (空值顯示為 "-")
- 🚧 錄入/編輯成績 (可擴展)
- 🚧 計算期末總成績 (可擴展)
- 🚧 統計分析 (可擴展)

### 3. 考試管理 (/tests)
- ✅ 查看考試列表
- ✅ 按學期篩選
- ✅ 考試狀態標籤顯示
- ✅ 權重百分比顯示
- 🚧 新增考試 (可擴展)
- 🚧 設定權重 (可擴展)
- 🚧 上傳考卷檔案 (可擴展)

### 4. 檔案管理
- ✅ 上傳檔案 API 整合
- ✅ 下載檔案 API 整合
- 🚧 UI 介面 (可擴展)

## API 整合狀態

### 學生 API (Student_MetadataWriter)
- ✅ POST /create - 創建學生
- ✅ POST /read - 查詢學生
- ✅ POST /update - 更新學生
- ✅ POST /delete - 刪除學生
- ✅ POST /status - 查詢狀態統計

### 成績 API (Score_MetadataWriter)
- ✅ POST /create - 創建/更新分數
- ✅ POST /read - 查詢成績
- ✅ POST /update - 更新成績
- ✅ POST /delete - 刪除成績
- ✅ POST /calculation_final - 計算期末總成績
- ✅ POST /test_score - 考試統計

### 考試 API (Test_MetadataWriter)
- ✅ POST /create - 創建考試
- ✅ POST /read - 查詢考試
- ✅ POST /update - 更新考試
- ✅ POST /delete - 刪除考試
- ✅ POST /status - 更新考試狀態
- ✅ POST /setweight - 設定考試權重

### 檔案 API (test-filedata)
- ✅ POST /create - 上傳檔案
- ✅ POST /read - 讀取檔案
- ✅ POST /update - 更新檔案
- ✅ POST /delete - 刪除檔案

## 快速開始

### 前置要求
- Node.js >= 18.x
- npm >= 9.x

### 安裝步驟

#### 1. 安裝 Node.js (如果尚未安裝)
```bash
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt-get install -y nodejs
```

#### 2. 進入專案目錄
```bash
cd /home/mitlab/project/front
```

#### 3. 使用快速啟動腳本
```bash
./start-dev.sh
```

或手動執行：

```bash
# 安裝依賴
npm install

# 啟動開發伺服器
npm run dev
```

#### 4. 訪問應用
開啟瀏覽器訪問: http://localhost:3000

## 可用指令

```bash
# 開發模式 (熱重載)
npm run dev

# 生產環境構建
npm run build

# 啟動生產伺服器
npm start

# ESLint 檢查
npm run lint

# TypeScript 類型檢查
npm run type-check
```

## 架構設計原則

### 1. 分層架構
```
┌─────────────────────────────────────┐
│          app/ (路由與組合)           │
│  - 路由定義                          │
│  - 頁面組合                          │
│  - Server Components                │
└──────────────┬──────────────────────┘
               │ imports
┌──────────────▼──────────────────────┐
│      components/ (UI 展示)          │
│  - 純展示邏輯                        │
│  - 格式化顯示                        │
│  - 展示層映射                        │
└──────────────┬──────────────────────┘
               │ imports
┌──────────────▼──────────────────────┐
│       hooks/ (狀態管理)             │
│  - 狀態管理                          │
│  - UI 互動邏輯                       │
│  - 視圖狀態映射                      │
└──────────────┬──────────────────────┘
               │ imports
┌──────────────▼──────────────────────┐
│     services/ (數據獲取)            │
│  - API 調用                          │
│  - DTO 標準化                        │
│  - 多 API 協調                       │
└─────────────────────────────────────┘
```

### 2. 類型安全
- 所有 API 回應都有完整的 TypeScript 類型定義
- Strict mode 啟用，確保類型安全
- 統一的 AsyncState 類型用於非同步狀態管理

### 3. 錯誤處理
- 統一的 API 錯誤處理機制
- 用戶友好的錯誤訊息顯示
- 完整的載入狀態管理

### 4. 代碼組織
- 每個模組職責單一明確
- 可重用的基礎元件和 hooks
- 功能特定的元件和 hooks 分開管理

## 擴展指南

### 添加新頁面
1. 在 `app/` 下創建新資料夾和 `page.tsx`
2. Next.js 自動識別為新路由

### 添加新 API 功能
1. 在 `types/` 定義 TypeScript 類型
2. 在 `services/api/` 創建 API 函數
3. 在 `hooks/feature/` 創建自訂 hook
4. 在 `components/` 創建 UI 元件
5. 在 `app/` 組合成頁面

### 添加新元件
1. 基礎元件放在 `components/` 根目錄
2. 功能元件放在 `components/{feature}/` 子目錄
3. 確保元件只負責展示邏輯

## 開發注意事項

### Server Components vs Client Components
- 預設使用 Server Components (更好的性能和 SEO)
- 需要使用狀態、事件處理、瀏覽器 API 時添加 `"use client"`
- API 調用通過 services 層，保持架構邊界

### 導入規則
- ✅ app/ 可導入: components, hooks, services, types, config
- ✅ components/ 可導入: hooks (功能元件), types, config
- ✅ hooks/ 可導入: services, types, config
- ✅ services/ 可導入: types, config
- ❌ services/ 不可導入 hooks 或 components
- ❌ hooks/ 不可導入 components

### 命名規範
- Services: `getX`, `listX`, `createX`, `updateX`, `deleteX`
- Hooks: `useAsync*`, `usePagination*`, `useXxxPage`
- Components: `PascalCase` (Button, StudentList 等)

## 待擴展功能

### 高優先級
- [ ] 學生編輯對話框
- [ ] 學生刪除確認對話框
- [ ] 成績錄入/編輯表單
- [ ] 考試新增對話框
- [ ] 權重設定介面

### 中優先級
- [ ] 分頁功能整合
- [ ] 排序功能整合
- [ ] 進階篩選
- [ ] 批量操作
- [ ] 數據導出功能

### 低優先級
- [ ] 深色模式
- [ ] 多語言支援
- [ ] 響應式優化 (移動端)
- [ ] 數據視覺化圖表
- [ ] 實時通知

## 環境變數

### 開發環境 (.env)
```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v0.1/Calculus_oom/Calculus_metadata
```

### 生產環境
需要設置：
```bash
NEXT_PUBLIC_API_BASE_URL=https://your-production-domain.com/api/v0.1/Calculus_oom/Calculus_metadata
```

## 疑難排解

### 依賴安裝失敗
```bash
rm -rf node_modules package-lock.json
npm install
```

### 端口被佔用
```bash
PORT=3001 npm run dev
```

### TypeScript 錯誤
```bash
npm run type-check
```

### ESLint 錯誤
```bash
npm run lint
```

## 性能優化

- ✅ Server Components 預設啟用
- ✅ 自動程式碼分割 (Next.js)
- ✅ 圖片優化 (Next.js Image)
- ✅ CSS 優化 (Tailwind CSS purge)
- 🚧 API 回應快取
- 🚧 無限滾動 / 虛擬滾動

## 安全性

- ✅ CSRF Token 支援 (待後端啟用)
- ✅ XSS 防護 (React 預設)
- ✅ 環境變數不提交 (.gitignore)
- 🚧 認證機制 (待實現)
- 🚧 權限控制 (待實現)

## 測試

目前暫無測試，建議添加：
- Unit Tests (Jest + React Testing Library)
- E2E Tests (Playwright / Cypress)
- API Integration Tests

## 文檔

- **README.md**: 專案概述
- **SETUP.md**: 詳細設置指南
- **frontend_architecture_prompt.md**: 架構規範
- **FRONTEND_to_backend_API_RULES.md**: API 對接規範
- **PROJECT_SUMMARY.md**: 本文件 - 完整說明

## 版本資訊

- **專案版本**: 0.1.0
- **Next.js**: 14.2.0
- **React**: 18.3.0
- **TypeScript**: 5.3.0

## 授權

MIT License

## 支援與貢獻

如需幫助或希望貢獻，請參考：
- Next.js 文檔: https://nextjs.org/docs
- React 文檔: https://react.dev
- TypeScript 文檔: https://www.typescriptlang.org/docs
- Tailwind CSS 文檔: https://tailwindcss.com/docs

---

**專案建立日期**: 2024-12-30
**最後更新**: 2024-12-30
**維護者**: MIT Lab Team
